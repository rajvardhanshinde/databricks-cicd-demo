bundle:
  name: "my-databricks-project"

variables:
  root_path:
    default: "/Shared/my-databricks-project"

resources:
  files:                      # requires unified CLI (0.205+)
    notebooks:
      source: ./Notebooks
      path: "${var.root_path}/${bundle.target}/Notebooks"

targets:
  dev:  { workspace: {} }
  ppe:  { workspace: {} }
  prod: { workspace: {} }
